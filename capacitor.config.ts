import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.campaign.lewis',
  appName: 'campaign',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
